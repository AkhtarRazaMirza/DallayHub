services:
  postgresdb:
    image: postgres:17
    ports:
      - '5432:5432'
    environment:
      POSTGRES_USER: akhtar
      POSTGRES_PASSWORD: akhtar
      POSTGRES_DB: SecureAuth
    volumes:
      - 'SecureAuth_data:/var/lib/postgresql/data'

volumes:
  SecureAuth_data: